const routes = {
  home: '/',
  login: '/login',
  register: '/register',
  admin: '/admin',
  adminLogin: '/admin/login',
  questions: '/questions',
  saves: '/saves',
  users: '/users',
  tags: '/tags'
}

export default routes
